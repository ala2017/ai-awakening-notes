---
import Base from '../layouts/Base.astro';
import { url } from '../lib/site';
---
<Base title="页面不存在 | AI 觉醒笔记">
  <header class="hero">
    <h1>这里没有页</h1>
    <p class="lede">你要找的东西不在这里。<a href={url()} style="color:var(--light)">回到笔记列表</a>。</p>
  </header>
</Base>
